#include <stdio.h>      /* printf, NULL */
#include <stdlib.h>     /* srand, rand */
#include <time.h>       /* time */

namespace Random_Number
{
class random_forcing
{
	public:
		int run1, run2;
		float ran1, ran2;
		float arr[int m];
		void Random_Seed(float k, float l);
		void Loop_Seed(int m);

};
}
